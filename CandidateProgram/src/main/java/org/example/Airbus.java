package org.example;

public class Airbus extends PassengersPlane {
    public Airbus(String name, int price, int capacity, double maxLoad, int flightDistance) {
        super(name, price, capacity, maxLoad, flightDistance);
    }
}
